{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main popup window: IPC to app commands and events only.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
